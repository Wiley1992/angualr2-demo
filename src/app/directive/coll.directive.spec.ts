import { CollDirective } from './coll.directive';

describe('CollDirective', () => {
  it('should create an instance', () => {
    const directive = new CollDirective();
    expect(directive).toBeTruthy();
  });
});
